name: CD - Publish Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g., 3.2.3)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run tests
        run: ./gradlew test

      - name: Verify coverage thresholds
        run: ./gradlew koverVerify

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python beancount
        run: |
          pip install beancount==3.2.3
          pip install beanquery

      - name: Run compatibility tests
        run: ./gradlew :loader:jvmTest --tests "*CompatTest" --tests "*CompatibilityVerificationTest"

  build-cli:
    name: Build CLI Distribution
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Extract version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
          else
            VERSION="${GITHUB_REF#refs/tags/v}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Building CLI version: $VERSION"

      - name: Build CLI distribution
        run: ./gradlew :cli:distZip --no-configuration-cache

      - name: Rename CLI artifact with version
        shell: bash
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          mv "modules/cli/build/distributions/beancount.zip" "modules/cli/build/distributions/beancount-cli-${VERSION}-jar.zip"

      - name: Upload CLI artifact
        uses: actions/upload-artifact@v4
        with:
          name: cli-distribution
          path: modules/cli/build/distributions/beancount-cli-*-jar.zip

  build-native-image:
    name: Build Native Image (${{ matrix.os }})
    needs: validate
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Set up GraalVM JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'graalvm'

      - name: Setup MSVC (Windows)
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Extract version
        id: version
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
          else
            VERSION="${GITHUB_REF#refs/tags/v}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Publishing version: $VERSION"

      - name: Build Native Images
        run: ./gradlew :cli:nativeCompile :cli:nativeBeanqueryCompile --no-configuration-cache

      - name: Package Native Images (Windows)
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          $OS = "${{ matrix.os }}"
          $OS_SHORT = $OS -replace '-latest$',''
          $VERSION = "${{ steps.version.outputs.version }}"
          New-Item -ItemType Directory -Force -Path native-release | Out-Null
          Copy-Item -Path modules/cli/build/native/nativeCompile/beancount* -Destination native-release -ErrorAction SilentlyContinue
          Copy-Item -Path modules/cli/build/native/nativeBeanqueryCompile/beanquery* -Destination native-release -ErrorAction SilentlyContinue
          Compress-Archive -Path native-release/* -DestinationPath "beancount-native-${VERSION}-${OS_SHORT}.zip" -Force

      - name: Package Native Images (Linux/macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mkdir -p native-release
          cp modules/cli/build/native/nativeCompile/beancount* native-release/ || true
          cp modules/cli/build/native/nativeBeanqueryCompile/beanquery* native-release/ || true
          OS="${{ matrix.os }}"
          OS_SHORT=$(echo "$OS" | sed -E 's/-latest$//')
          VERSION="${{ steps.version.outputs.version }}"
          cd native-release && zip -r "../beancount-native-${VERSION}-${OS_SHORT}.zip" .

      - name: Upload Native Image artifacts
        uses: actions/upload-artifact@v4
        with:
          name: native-${{ matrix.os }}
          path: beancount-native-*.zip

  publish-maven:
    name: Publish to Maven Central
    needs: validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Extract version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
          else
            VERSION="${GITHUB_REF#refs/tags/v}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Publishing version: $VERSION"

      - name: Decode signing key
        run: |
          printf '%s' '${{ secrets.SIGNING_KEY }}' | base64 -d | gpg --dearmor > ${{ github.workspace }}/signing-key.gpg
          ls -l ${{ github.workspace }}/signing-key.gpg
          gpg --list-packets ${{ github.workspace }}/signing-key.gpg | head -5

      - name: Publish to Maven Central
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
        run: |
          ./gradlew publishAndReleaseToMavenCentral \
            --no-configuration-cache \
            -Pversion=${{ steps.version.outputs.version }} \
            -PRELEASE_SIGNING_ENABLED=true \
            -Psigning.keyId=${{ secrets.SIGNING_KEY_ID }} \
            -Psigning.password=${{ secrets.SIGNING_PASSWORD }} \
            -Psigning.secretKeyRingFile=${{ github.workspace }}/signing-key.gpg

  create-release:
    name: Create GitHub Release
    needs: [build-cli, build-native-image, publish-maven]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
          else
            VERSION="${GITHUB_REF#refs/tags/v}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Creating release for version: $VERSION"

      - name: Download CLI artifact
        uses: actions/download-artifact@v4
        with:
          name: cli-distribution
          path: release-artifacts

      - name: Download Native Image artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: native-*
          merge-multiple: true
          path: release-artifacts

      - name: List release artifacts
        run: ls -la release-artifacts/

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "v${{ steps.version.outputs.version }}" \
            --title "v${{ steps.version.outputs.version }}" \
            --generate-notes \
            release-artifacts/*

      - name: Update CHANGELOG
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          DATE=$(date +%Y-%m-%d)
          python3 - "$VERSION" "$DATE" << 'PYEOF'
          import sys
          version = sys.argv[1]
          today = sys.argv[2]
          with open('CHANGELOG.md', 'r') as f:
              content = f.read()
          # Insert new version section after [Unreleased]
          content = content.replace(
              '## [Unreleased]',
              f'## [Unreleased]\n\n## [{version}] - {today}'
          )
          with open('CHANGELOG.md', 'w') as f:
              f.write(content)
          PYEOF

      - name: Commit CHANGELOG update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add CHANGELOG.md
          git diff --cached --quiet || git commit -m "docs: update CHANGELOG for v${{ steps.version.outputs.version }}"
          git push
